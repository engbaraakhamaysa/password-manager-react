export default function Settings() {
  return (
    <div>
      <h1>Settings</h1>

      <div>
        <p>Account Settings</p>

        <button>Change Password</button>
      </div>
    </div>
  );
}
